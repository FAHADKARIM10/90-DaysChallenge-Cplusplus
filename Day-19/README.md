Inheritance the fundemental concept of oop .It is a mechanism by which one class can inherit the properties features field and methods of another class.inheritance means creating new classes 

A class that inherit from another class another class can reuse the methods and fields of that class 
